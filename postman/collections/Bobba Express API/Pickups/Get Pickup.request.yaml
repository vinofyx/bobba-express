$kind: http-request
name: Get Pickup
method: GET
url: '{{baseUrl}}/api/pickups/{{pickupId}}'
description: Get a single pickup by ID.
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
