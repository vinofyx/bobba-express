$kind: http-request
name: Get Dashboard
method: GET
url: '{{baseUrl}}/api/dashboard'
description: Get the main dashboard overview. Admin/staff only.
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
