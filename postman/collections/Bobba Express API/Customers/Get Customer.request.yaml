$kind: http-request
name: Get Customer
method: GET
url: '{{baseUrl}}/api/customers/{{customerId}}'
description: Get a single customer by ID.
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
