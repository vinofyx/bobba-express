$kind: http-request
name: Get Shipment
method: GET
url: '{{baseUrl}}/api/shipments/{{shipmentId}}'
description: Get a single shipment by ID.
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
